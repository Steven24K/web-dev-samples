{
  "name": "project",
  "version": "1.0.0",
  "description": "Sample HTML project with Parcel development server",
  "scripts": {
    "start": "npx parcel ./src/index.html",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Steven",
  "license": "ISC",
  "devDependencies": {
    "parcel": "^2.15.4"
  }
}
